//This is the beginning of the strack class

//push method
if empty, set head to newNode
else
set temp to head
newNode.setNext(temp)
head = newNode

public Node pop(){
Node tmp = this.top;
this.top = top.next;
return tmp;
}
